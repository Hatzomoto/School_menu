<nav class="navbar fixed-top navbar-expand-lg navbar-light" style="background-color: #00a9c7;">
    <div class="container-fluid">
        <h4 class="text-light font-weight-bolder">Hello {{usuario}}</h4>
        <div>
            <button onclick="logout()" type="button" class="btn btn-danger btn-lg px-4">Logout</button>
        </div>
    </div>
</nav>

<script>

    const logout = () => {

        Swal.fire({
            title: 'Are you sure?',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes'
        }).then(async (result) => {
            if (result.isConfirmed) {
                const { data } = await axios.get('/logout');
                setTimeout(() => window.location.href = '/login', 1000)
                
            }
        })
    }

</script>